import { MigrationInterface, QueryRunner, Table } from 'typeorm';

export class CreateUsersTable1700000000000 implements MigrationInterface {
  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.createTable(
      new Table({
        name: 'users',
        columns: [
          {
            name: 'id',
            type: 'uuid',
            isPrimary: true,
            generationStrategy: 'uuid',
            default: 'uuid_generate_v4()',
          },
          { name: 'name', type: 'varchar', length: '255' },
          { name: 'email', type: 'varchar', length: '255', isUnique: true },
          { name: 'password', type: 'varchar' },
          { name: 'reset_password_token', type: 'varchar', isNullable: true },
          { name: 'reset_password_expiry', type: 'timestamp', isNullable: true },
{{#isTrue rbac}}
          { name: 'role', type: 'varchar', length: '20', default: "'user'" },
{{/isTrue}}
{{#isTrue twoFactor}}
          { name: 'two_factor_enabled', type: 'boolean', default: false },
          { name: 'two_factor_secret', type: 'varchar', isNullable: true },
{{/isTrue}}
{{#ne multiTenancy "none"}}
          { name: 'tenant_id', type: 'uuid', isNullable: false },
{{/ne}}
          { name: 'created_at', type: 'timestamp', default: 'now()' },
          { name: 'updated_at', type: 'timestamp', default: 'now()' },
        ],
      }),
      true,
    );
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.dropTable('users');
  }
}
